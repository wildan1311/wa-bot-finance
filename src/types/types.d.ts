interface FormattedChat {
    date: string;
    contact?: string; // Optional field for contact name
    description: string;
    category: string;
    nominal: number;
}

export type { FormattedChat };